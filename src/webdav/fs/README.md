# fs — WebDAV Filesystem Operations

Implements low-level filesystem operations for the WebDAV module.

| File | Responsibility |
|------|----------------|
| `copy_engine.c` | Confined file and directory copy implementation, including xattr preservation |
| `copy_engine.h` | FS copy engine types and prototypes |
