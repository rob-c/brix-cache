/* src/platform/linux/host_api.h - Linux has no host-only PAL extensions.
 * Reached through platform/platform_api.h; kept so every host answers the
 * same computed include. */
#ifndef BRIX_PLATFORM_LINUX_HOST_API_H
#define BRIX_PLATFORM_LINUX_HOST_API_H
#endif
