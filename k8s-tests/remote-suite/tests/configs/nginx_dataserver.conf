# Dedicated nginx configuration for data-server in cluster tests.
#
# Standalone XRootD server that registers with the redirector's CMS port via
# brix_cms_manager. Advertises export paths and sends periodic heartbeat/
# availability frames to the redirector.

worker_processes 1;
error_log {LOG_DIR}/error.log debug;
pid       {LOG_DIR}/nginx.pid;

events { worker_connections 128; }

stream {
    server {
        listen {BIND_HOST}:{PORT};
        xrootd on;
        brix_storage_backend posix:{DATA_DIR};
        brix_auth none;
        brix_cms_manager {BIND_HOST}:{CMS_PORT};
        brix_cms_paths /;
        brix_cms_interval 5;
        brix_listen_port {PORT};
    }
}
