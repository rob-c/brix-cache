[pytest]
# k8s-tests Python port. Fast tier by default; e2e needs a live cluster.
#   pytest -m 'not e2e'   # fast: dry-run, config-shape, klib unit (no cluster)
#   pytest -m e2e         # real minikube deploys
markers =
    e2e: needs a live minikube cluster
addopts = -ra
testpaths = .
