apiVersion: v2
name: cms-mesh
description: CMS cluster — a manager (redirector) and a data-server that registers with it
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - { name: topology-role, version: 0.1.0, repository: file://../topology-role, alias: manager }
  - { name: topology-role, version: 0.1.0, repository: file://../topology-role, alias: ds }
