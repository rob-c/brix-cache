worker_processes 1;
events { worker_connections 64; }
http {
  server {
    listen 8080;
    root /srv/dist;
    autoindex on;
    location /healthz { return 200 "ok\n"; }
    location /crl/   { }
    location /certs/ { }   # jwks.json + ca bundle
    location /voms/  { }
  }
}
