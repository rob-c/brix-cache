# fail2ban filter — nginx xrootd/ARC guard audit log, signal=signature lines.
# Line format (src/net/guard/guard_audit.c — the fail2ban contract):
#   <iso8601> ip=<addr> proto=<p> signal=signature op=<op> path="<path>" status=<n>
[Definition]
failregex = ^\S+ ip=<HOST> proto=\S+ signal=signature\b
ignoreregex =
datepattern = ^%%Y-%%m-%%dT%%H:%%M:%%S
