# Reference xrootd — anonymous data server (rendered by refxrootd.sh:write_ref_cfg).
# Placeholders {PORT} {DATA_DIR} {ADMIN_DIR} {RUN_DIR} filled by render_cfg (util.sh).
xrd.port {PORT}
oss.localroot {DATA_DIR}
all.export /
all.adminpath {ADMIN_DIR}
all.pidpath   {RUN_DIR}
xrd.sched mint 2 maxt 4 avlt 2
xrd.trace off
