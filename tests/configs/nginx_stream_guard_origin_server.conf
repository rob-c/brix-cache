# Server block for the origin in test_stream_guard.py.
# Placeholders: {BIND_HOST} {PORT} {EXPORT_DIR}
    server {
        listen {BIND_HOST}:{PORT};
        brix_root on;
        brix_export {EXPORT_DIR};
        brix_auth none;
    }
