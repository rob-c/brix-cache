worker_processes 1;
error_log {LOG_DIR}/error.log info;

thread_pool default threads=2 max_queue=256;
events { worker_connections 64; }

http {
    client_body_temp_path {TMP_DIR};
    proxy_temp_path       {TMP_DIR};
    fastcgi_temp_path     {TMP_DIR};
    uwsgi_temp_path       {TMP_DIR};
    scgi_temp_path        {TMP_DIR};
    sendfile on;
    client_max_body_size 1g;

    server {
        listen {PORT} ssl;
        server_name localhost;

        ssl_certificate     {SERVER_CERT};
        ssl_certificate_key {SERVER_KEY};

        ssl_conf_command Options KTLS;
        ssl_buffer_size  64k;

        access_log {LOG_DIR}/webdav_token_access.log;

        location / {
            brix_webdav         on;
            brix_storage_backend posix:{DATA_DIR};
            brix_allow_write    on;
            # brix_webdav_auth required mandates a CA store even in token-only
            # mode (used for proxy-cert path; token is the primary auth here).
            brix_webdav_cafile  {CA_CERT};
            brix_webdav_auth    required;

            brix_webdav_token_jwks     {TOKEN_DIR}/jwks.json;
            brix_webdav_token_issuer   "https://test.example.com";
            brix_webdav_token_audience "nginx-xrootd";
        }
    }
}
