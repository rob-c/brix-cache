# Self-contained network-fault stream template for test_netfault_stream.py.
# Placeholders: {LOG_DIR} {HOST} {PORT} {DATA_DIR} {HANDSHAKE_TIMEOUT_MS}
#               {READ_TIMEOUT_MS} {SEND_TIMEOUT_MS}
daemon off;
master_process off;
error_log {LOG_DIR}/error.log info;
pid {LOG_DIR}/nginx.pid;

events { worker_connections 256; }

stream {
    server {
        listen {HOST}:{PORT};
        brix_root on;
        brix_storage_backend posix:{DATA_DIR};
        brix_handshake_timeout {HANDSHAKE_TIMEOUT_MS}ms;
        brix_read_timeout {READ_TIMEOUT_MS}ms;
        brix_send_timeout {SEND_TIMEOUT_MS}ms;
        brix_tcp_keepalive on;
        brix_tcp_user_timeout 30s;
    }
}
