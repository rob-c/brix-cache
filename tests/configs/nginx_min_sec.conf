error_log {LOG_DIR}/error.log info;
pid {LOG_DIR}/nginx.pid;
daemon on;
events { worker_connections 64; }

stream {
    server {
        listen {BIND_HOST}:{PORT};
{TLS_LINES}        brix_root on;
        brix_storage_backend posix:{DATA_ROOT};
        brix_auth {AUTH};
        brix_min_sec_level {MIN_SEC};
    }
}
