# Server block for guarded/unguarded relays in test_stream_guard.py.
# Placeholders: {BIND_HOST} {PORT} {ORIGIN_PORT} {GUARD_DIRECTIVE}
    server {
        listen {BIND_HOST}:{PORT};
        brix_root on;
        brix_transparent_proxy {BIND_HOST}:{ORIGIN_PORT};
        {GUARD_DIRECTIVE}
    }
