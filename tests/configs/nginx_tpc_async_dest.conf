# Native TPC destination — registry-lifecycle template for test_tpc_async_open.py.
# Pulls from a source that answers the open asynchronously (kXR_wait /
# kXR_waitresp→kXR_attn); exercises tpc_open_resolve()'s deferred-open handling.
# Launcher-provided: LOG_DIR PORT DATA_ROOT.
daemon on;
worker_processes 1;
error_log {LOG_DIR}/dst-err.log info;
pid {LOG_DIR}/nginx.pid;

thread_pool default threads=4 max_queue=65536;

events { worker_connections 64; }

stream {
    server {
        listen 127.0.0.1:{PORT};
        brix_root on;
        brix_storage_backend posix:{DATA_ROOT};
        brix_auth none;
        brix_allow_write on;
        brix_tpc_allow_local on;
        brix_tpc_allow_private on;
        brix_access_log {LOG_DIR}/dst-acc.log;
    }
}
