# Stream guard test wrapper for test_stream_guard.py.
# Placeholders: BASE_DIR SERVER_BLOCKS
daemon on;
worker_processes 1;
error_log {BASE_DIR}/error.log info;
pid {BASE_DIR}/nginx.pid;

events { worker_connections 64; }

stream {
{SERVER_BLOCKS}
}
