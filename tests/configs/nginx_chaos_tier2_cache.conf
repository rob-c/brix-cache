# Dedicated Chaos Mesh Tier2: read-through cache between Tier1 proxy and Tier3 storage.

worker_processes 1;
error_log {LOG_DIR}/error.log debug;
pid       {LOG_DIR}/nginx.pid;

thread_pool default threads=4 max_queue=65536;

events { worker_connections 512; }

stream {
    server {
        listen {BIND_HOST}:{PORT};
        xrootd on;
        xrootd_root {DATA_DIR};
        xrootd_auth none;

        xrootd_cache on;
        xrootd_cache_root {CACHE_DIR};
        xrootd_cache_origin localhost:{UPSTREAM_PORT};
    }
}
