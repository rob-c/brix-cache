# Backend capability negatives against the real sd_http driver, for
# test_backend_caps_negative.py (registry lifecycle).
#
# One WebDAV origin plus two root:// exports storing THROUGH it, so every
# capability the http backend does not advertise (no CAP_XATTR/CAP_XATTR_WRITE,
# no CAP_TRUNCATE) is refused by the VFS gate on a real driver struct rather than
# a synthetic one.  The two exports differ only in `brix_stage`: with the default
# write-stage tier the xattr call reaches the leaf through the composed
# decorator (missing slot -> ENOSYS at the seam), with `brix_stage off` it hits
# sd_http as the top driver (missing slot -> ENOTSUP in the VFS).  Both spellings
# of "this backend has no xattrs" must produce the same client-visible answer.
#
# worker_processes 2: both root:// exports connect back to the WebDAV origin
# served by this same nginx — a single worker blocked in a synchronous backend
# call could never accept its own connection.
#
# Launcher-provided: LOG_DIR TMP_DIR PORT ORIGIN_PORT STAGE_OFF_PORT.
# Test-provided: BIND_HOST ORIGIN_DATA EXPORT_STAGED EXPORT_DIRECT.
worker_processes 2;
pid {LOG_DIR}/nginx.pid;
error_log {LOG_DIR}/error.log info;
daemon on;

events { worker_connections 64; }

http {
    access_log off;
    client_max_body_size 16m;
    client_body_temp_path {TMP_DIR}/cbt;
    proxy_temp_path       {TMP_DIR}/pt;
    fastcgi_temp_path     {TMP_DIR}/ft;
    uwsgi_temp_path       {TMP_DIR}/ut;
    scgi_temp_path        {TMP_DIR}/st;

    server {                                    # the ORIGIN of record
        listen {BIND_HOST}:{ORIGIN_PORT};
        location / {
            brix_webdav on;
            brix_export {ORIGIN_DATA};
            brix_webdav_auth none;
            brix_allow_write on;
        }
    }
}

stream {
    server {                                    # http backend UNDER a stage tier
        listen {BIND_HOST}:{PORT};
        brix_root on;
        brix_export {EXPORT_STAGED};
        brix_auth none;
        brix_allow_write on;
        brix_storage_backend http://{BIND_HOST}:{ORIGIN_PORT};
    }

    server {                                    # http backend as the top driver
        listen {BIND_HOST}:{STAGE_OFF_PORT};
        brix_root on;
        brix_export {EXPORT_DIRECT};
        brix_auth none;
        brix_allow_write on;
        brix_stage off;
        brix_storage_backend http://{BIND_HOST}:{ORIGIN_PORT};
    }
}
