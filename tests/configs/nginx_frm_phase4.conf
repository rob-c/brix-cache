# FRM phase-4 directives template for test_frm_phase4.py.
worker_processes 1;
error_log {BASE_DIR}/logs/error.log info;
pid {BASE_DIR}/logs/nginx.pid;

events { worker_connections 64; }

stream {
    server {
        listen {BIND_HOST}:{PORT};
        brix_root on;
        brix_storage_backend posix:{DATA_DIR};
        brix_auth none;
        brix_frm on;
        brix_frm_queue_path {QUEUE_PATH};
        brix_frm_stagecmd /bin/true;
        brix_frm_max_per_source 2;
        brix_frm_residency_cmd /bin/true;
        brix_frm_migrate_copycmd /bin/true;
        brix_frm_purge_watermark 0.90 0.80;
        brix_frm_purge_interval 1s;
    }
}

http {
    access_log off;
    server {
        listen {BIND_HOST}:{METRICS_PORT};
        location = /metrics { brix_metrics on; }
    }
}

daemon off;
master_process off;
