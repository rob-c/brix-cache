worker_processes 1;
error_log {LOG_DIR}/error.log info;

events { worker_connections 64; }

stream {
    server {
        listen 127.0.0.1:{PORT};
        xrootd on;
        xrootd_auth         none;
        xrootd_supervisor   on;
        xrootd_manager_mode on;
    }
}
