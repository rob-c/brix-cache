# WebDAV macaroon server WITH a rotation grace secret (registry lifecycle),
# the brix_webdav_macaroon_secret_old twin of nginx_lc_webdav_macaroon.conf
# (a separate template: rendering is non-strict, so an optional slot there
# would break its existing callers).
# Launcher-provided: LOG_DIR TMP_DIR PORT.
# Test-provided via template_values (dirs seeded under tmp_path, must exist at
# config-parse time): BIND_HOST DATA_DIR CADIR SECRET_HEX OLD_SECRET_HEX.
worker_processes 1;
pid {LOG_DIR}/nginx.pid;
error_log {LOG_DIR}/error.log info;
daemon on;

events { worker_connections 64; }

http {
    client_body_temp_path {TMP_DIR}/cbt;
    proxy_temp_path       {TMP_DIR}/pt;
    fastcgi_temp_path     {TMP_DIR}/ft;
    uwsgi_temp_path       {TMP_DIR}/ut;
    scgi_temp_path        {TMP_DIR}/st;
    access_log off;

    server {
        listen {BIND_HOST}:{PORT};
        location / {
            brix_webdav on;
            brix_storage_backend posix:{DATA_DIR};
            brix_webdav_auth required;
            brix_webdav_cadir {CADIR};
            brix_allow_write on;
            brix_webdav_macaroon_secret {SECRET_HEX};
            brix_webdav_macaroon_secret_old {OLD_SECRET_HEX};
        }
    }
}
