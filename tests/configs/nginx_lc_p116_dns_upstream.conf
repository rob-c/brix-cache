# Phase-116 async-upstream template (registry lifecycle) for
# tests/test_phase116_upstream_async_resolve.py.  Launcher-provided: LOG_DIR
# TMP_DIR DATA_ROOT PORT ROOT_PORT.  Test-provided: BIND_HOST RESOLV_CONF
# RESOLVER_EXTRA RETRY UPSTREAM_HOST UPSTREAM_PORT.  brix_upstream names a host
# the stub nameserver controls: unresolvable at start, then swapped to a live
# Python listener — the dial must follow the record, never block the loop.
worker_processes 1;
thread_pool default threads=2 max_queue=256;
pid {LOG_DIR}/nginx.pid;
error_log {LOG_DIR}/error.log info;
daemon on;

events { worker_connections 256; }

stream {
    brix_resolver auto path={RESOLV_CONF} {RESOLVER_EXTRA};
    brix_dns_retry {RETRY};

    server {
        listen {BIND_HOST}:{ROOT_PORT};
        brix_root on;
        brix_storage_backend posix:{DATA_ROOT};
        brix_auth none;
        brix_upstream {UPSTREAM_HOST}:{UPSTREAM_PORT};
    }
}

http {
    access_log off;
    client_body_temp_path {TMP_DIR};
    proxy_temp_path       {TMP_DIR};
    fastcgi_temp_path     {TMP_DIR};
    uwsgi_temp_path       {TMP_DIR};
    scgi_temp_path        {TMP_DIR};

    server {
        listen {BIND_HOST}:{PORT};
        location /metrics { brix_metrics on; }
        location /brix/ {
            brix_dashboard on;
            brix_admin_rate_limit off;
            brix_dashboard_password "p116-dashboard";
            brix_dashboard_anonymous on;
        }
    }
}
