# Anchors pytest's rootdir at BriXTest/ so its own tests run
# self-contained: the repository-root conftest.py is never loaded here.
[pytest]
testpaths = tests compat_tests examples
markers =
    unit: fast BriXTest internal contract test
    integration: opt-in test requiring external runtimes or services
