# Development History — Build System, Packaging, Refactors & Working Agreements

**Status:** Living record — synthesized from session memory 2026-04 through 2026-07-15.
**Scope:** Build/packaging mechanics and gotchas, codebase-wide refactor waves (renames, layout moves, god-header/complexity burndowns), and the standing operating rules for AI agents working in this repo.
**Related:** [coding-standards.md](coding-standards.md) · [lessons-codebase-hardening-2026-06.md](lessons-codebase-hardening-2026-06.md) · [lessons-brix-rebrand-and-suite-stabilization-2026-07.md](lessons-brix-rebrand-and-suite-stabilization-2026-07.md) · [postmortem-refactor-regressions.md](postmortem-refactor-regressions.md) · [postmortem-origin-credential-shadowing.md](postmortem-origin-credential-shadowing.md) · CLAUDE.md (top-level hard rules — this doc adds detail and incidents, it does not replace it)

---

# Part 1 — Engineering history

## 1. Build system mechanics — the recurring traps

Three separate facts about this build kept re-costing sessions hours, across a two-month span, because each looks like a different symptom:

1. **The compiled-source list lives in the repo-root `./config` script**, not `src/config/config.h` (the latter only holds config-struct/directive declarations). New `.c` files go in `./config`'s `ngx_module_srcs`/per-feature blocks, then `./configure` must re-run. CLAUDE.md's older wording pointing at `config.h` was corrected in-repo once this was pinned down precisely (2026-06-19 era).
2. **The addon build does not track cross-file header dependencies.** Editing any header that changes a struct's layout (a field insert, a field-count change, a new pointer) and then running an *incremental* `make` silently links a **mixed-ABI binary**: some `.o` files see the old layout, some the new, so field reads land at the wrong offset. This is NOT always a crash — it has repeatedly produced silently-wrong behavior (writes "succeed" but don't land, flags read as garbage) that masqueraded as logic bugs and cost full multi-hour-to-multi-day debugging spirals before the real cause (a stale `.o`) was found. Confirmed recurrences: the Phase-1 output-queue refactor (`xrootd_ctx_t` resize, "Phase-29 corruption", actually a stale-object illusion since June); a writethrough→sd_stage fold "durability bug" (July, actually ABI-corrupt incremental build); the July `csi_tagstore.h` struct-size change during the brix-rebrand suite burndown (402 worker SIGABRTs across the fleet, diagnosed via `.o` mtimes older than the header).
   **Standing rule, reconfirmed every time:** after any change to a struct laid out in a header, do a full clean rebuild — `rm -rf objs && ./configure && make` (never `make clean`, which on `shared/xrdproto` does NOT remove `build/*.o` either — needs an explicit `rm -rf`). If in doubt whether a rebuild actually happened, check that `objs/nginx`'s mtime/size actually changed.
3. **`REPO=... ./configure --add-module=$REPO` on one line silently expands `$REPO` empty** (the same-line assignment isn't visible to same-line expansion) — configure then builds a bare nginx with the module dropped, `make` exits 0 with zero warnings, and a live test fleet keeps serving its old good binary so pytest even keeps passing. It only surfaces on a fleet **restart** (`nginx: [emerg] unknown directive "brix_root"`). Always `export REPO=...` as its own statement, or use the literal path. Mandatory post-rebuild verification: configure log says "adding module in <path>"; `strings objs/nginx | grep -cx brix_root` ≥ 1; a known split object file exists under `objs/addon/`. A clean `make` exit 0 alone is not evidence the module actually compiled in.

Related, narrower build facts:
- The module's `config` CFLAGS are **global** — they apply to nginx core too (nginx links one CFLAGS for everything, and CLAUDE.md forbids patching nginx source), so any `-Werror`-promoted warning flag added to `config` must be clean against nginx core itself. `-Wvla`/`-Wstrict-prototypes`/`-Wold-style-definition` are in; noisier flags (`-Wshadow`, `-Wcast-qual`, `-Wconversion`, `-Wsign-conversion`, `-Wformat=2`) are opt-in only via `--with-cc-opt` for module-only audits.
- **Three separate build systems compile overlapping `src/` files**, and a file-size split of a shared file must satisfy all three or it fails silently in a fourth place: (1) the nginx module via `./config`; (2) `shared/xrdproto/Makefile`, which builds the ngx-free `libxrdproto.a` from explicit `*_OBJS` lists — a split `.c` whose object belongs in this archive needs its sibling objects added there too; (3) the client build, where native tools and especially the `LD_PRELOAD` POSIX shim `libbrixposix_preload.so` whole-archive `libxrdproto.a`, so a missing split object surfaces only as an undefined-symbol-at-load-time failure in `tests/test_xrootdfs.py -k preload`. `make -C shared/xrdproto check` does **not** catch a missing object (its self-test only exercises the checksum core) — this bit twice during the phase-79 complexity burndown (`gsi_core`/`proxy_req` and `sd_s3` splits both silently broke the archive, caught only much later by the preload test). Mandatory check after any such split: grep the `ar rcs libxrdproto.a` object list, add siblings, then `nm -D --undefined-only client/libbrixposix_preload.so | grep -iE 'brix_|sd_|gsi_'` must be empty.
- **The client build has the same "every source must be listed" promise as `./config`, and it rotted silently (2026-08-03).** `client/Makefile` says of itself "every `.c` must be listed (no wildcards)", but 33 client TUs and 8 `shared/cvmfs` TUs — every one of them a phase-38/-69 **split sibling whose parent was listed** — were named by nothing and therefore compiled by nothing. Unlike a missing `./config` entry, this has no clean symptom: it surfaces as a link-time `undefined reference` in whichever binary happens to call into the orphaned half (`battery_s3`, `brixautofs_main`, `brix_cas_pack_*`, `cvmfs_xorf_*`, …), so `make -C client` was simply **red on `main`** with nothing naming the cause. The failure mode is structural — splitting a file for the size cap is exactly when you add a sibling and forget the build list — so it is now guarded by `tools/ci/check_client_build_coverage.py`, the client-side twin of `check_config_coverage.py` (drivers named `*_unit.c`/`*_unittest.c` and the `client/tests|examples|bin/` trees are excused; the allowlist is empty and should stay empty). Note the asymmetry that lets the guard be one-directional: a `./config` entry for a deleted file is silently ignored, but a Makefile prerequisite with no source is already a hard `make` error naming the file, so only the forward direction can fail quietly.
- **A compiler warning was reporting a real bug, and the obvious "fix" would have hidden it (2026-08-03).** `make -C client` succeeded but emitted two `-Wformat-truncation=` warnings from `client/apps/fs/brixcvmfs_transport.c`: `to_https()` rewrote `http://` → `https://` with `snprintf` into a destination the same size as the source and ignored the return value, so GCC computed the output as "between 9 and 1025 bytes into a destination of size 1024". The cheap ways to silence it — casting the result to `(void)`, or adding `-Wno-format-truncation` — would have left the defect: a **truncated URL is a different URL**, so the TLS probe would `GET` an object nobody asked for and the resulting 4xx would be misread as "this mirror has no TLS", silently demoting the mirror to cleartext. The fix was semantic: consume `snprintf`'s return and report *not rewritten* when it would not fit (the caller then uses the plain http URL against the **right** object), guard the `httpurl` build the same way (a truncation there means fetching a different object and hash-failing it, so the mirror is failed instead), and size the https buffer `BRIXCVMFS_URL_HTTPS_MAX = BRIXCVMFS_URL_MAX + 1` — since `s/http:/https:/` adds exactly one byte, anything that fitted as http always fits as https, which makes the refusal path unreachable for the transport's own longest legal URL *and* removes GCC's computed range overlap. Locked in by the `cvmfs_url_rewrite` C unit (`tests/c/cvmfs_url_rewrite_test.c`), which `#include`s the TU to reach the statics, stubs its ten project externals so nothing touches the network, and compiles `-O2 -D_FORTIFY_SOURCE=2 -Wall -Wextra -Werror` — the exact shape that warned, so the warning's return is a test failure rather than build noise. General rule: `-Wformat-truncation` at `-Wall`'s level fires only when the return value is unused, which means it is nearly always pointing at a *silent* truncation the code has no branch for.
- Dynamic RPM modules need their libs in `ngx_module_libs`, not just `CORE_LIBS` — `CORE_LIBS` only links the nginx binary; each dynamic `.so` links against `ngx_module_libs`, and a lib present only in `CORE_LIBS` produces a `.so` that `dlopen`s with an undefined symbol on stock nginx. `ngx_module_libs` is also not reset between `. auto/module` blocks — set it explicitly every block, or one module silently inherits another's libs.
- Cookie API differs by nginx version (`headers_in.cookie` vs `.cookies` array, the 1.23.0 boundary) — the only version-guarded site in `src/` is `src/dashboard/auth.c`'s cookie finder.
- **C unit tests link a hermetic object closure, so nginx's own helpers are not
  there.** Reaching for `ngx_cpystrn` (or any `ngx_*` string/pool helper) in code
  that a unit test compiles standalone produces an undefined reference at link
  time even though the module build is clean — use plain libc (`snprintf`) in
  any TU a unit driver `#include`s or links. Three more recurring shapes in the
  same family:
  - The unit drivers compile with a blanket `-Werror` that the real build does
    not use, so `-Wformat-truncation` is *fatal there and only there*. Cap the
    offending field explicitly (`%.72s` into `f[5][80]`) rather than widening
    the buffer by guesswork.
  - The client `Makefile` already passes `-D_GNU_SOURCE`, so a TU that defines
    it again is a redefinition warning → error. Guard with `#ifndef`.
  - Adding a call inside a fanned-out function obliges every existing unit
    driver that stubs that TU's externals to gain the new stubs too; the link
    error names them, but the fix belongs in the driver, not in the product
    code.
- **An unbalanced double quote in the repo-root `config` silently drops
  sources.** nginx's `auto/configure` *sources* the file with `.`, and the shell
  is lax: configure limps past the broken quote, still prints "+
  ngx_stream_brix_module was configured", and produces a working binary — but
  every source after the stray quote is gone from that module's
  `ngx_module_srcs`. The instance that caught it (fixed in `7c773d633`) was a
  file-size split that added its new sibling as a fresh
  `ngx_module_srcs="$ngx_addon_dir/.../ftp_module_gsi.c \` line, duplicating the
  `ngx_module_srcs="` prefix instead of writing a bare continuation
  `$ngx_addon_dir/... \`, thereby opening a second unterminated quote. The
  tell-tale was the binary *growing* ~400 KB (29.9 M → 30.3 M) once the dropped
  gridftp `ev/*.c` sources finally compiled in. **Rule: run `bash -n config`
  after any edit to `config`** — HEAD is clean, so a fresh failure is yours.
  `bash -n` catches this; a successful `make` does not. One
  `ngx_module_srcs="` per block, bare continuation lines, last line closes the
  quote.
- **A harness or unit driver that compiles a config struct must carry the same
  `-DBRIX_HAVE_*` defines as the module build.** Every feature gate
  (`LIBXML2`, `JANSSON`, `KRB5`, `SECCOMP`, `ZLIB`, `ZSTD`, `LZMA`, `BROTLI`,
  `BZIP2`, `LZ4`, `SQLITE`) adds fields to `ngx_stream_brix_srv_conf_t`, so a
  sub-struct that sits *past* them lands at a different offset in a harness
  compiled without them. This is the mixed-ABI failure above, arriving through a
  second build system — it presents as a SIGSEGV in the harness, not as a
  compile error.
- **nginx wipes the worker environment.** Anything the runtime must read from
  the environment needs an explicit `env NAME;` in the config (the outbound
  krb5 leg needs `env KRB5_CONFIG; env KRB5_KTNAME;` to find the KDC at all).
  The corollary for test doubles: a mock `.so` that captures its configuration
  in an `__attribute__((constructor))` reads it at **dlopen time in the master**,
  during config parse — a plain worker-side `getenv()` for the same variable
  comes back empty.
- **`.gitignore` blanket-ignores `tools/*`**, so a CI guard that walks the tree
  with `rglob` picks up ignored, half-written scratch files and dies on them.
  Enumerate with `git ls-files --cached --others --exclude-standard` instead —
  that is the same set a fresh clone would see, which is what a guard is
  actually asserting about.
- **De-static'ing a helper to share it across a split can collide with a global
  already in `libbrix.a`.** The symptom is a duplicate-symbol link error in one
  binary only (whichever links both). Prefix uniquely when promoting a former
  `static` to external linkage.
- **A verbatim file split can silently re-key a path-keyed static-analyzer
  baseline finding onto the new sibling**, so CI fails on a finding that is
  byte-identical to one already suppressed — and local stays green if the local
  clang does not reproduce it. Bound it by treating CI-only baseline entries as
  legitimate and re-keying them as part of the split, not as a new defect.
- **`minikube image load` does not always replace a cached image** (buildkit
  writes a manifest list, and the load is a no-op against the existing tag), so
  a rebuilt server image silently keeps serving the old binary. Use
  `minikube image rm` followed by `docker save … | minikube ssh -- docker load`,
  and verify by **config digest**, never by tag name.
- `-fno-semantic-interposition` measured as a no-op on this non-PIE build; `-ffunction-sections/-fdata-sections/--gc-sections` measured as not worth it (nginx's `-Wl,-E` pins ~2400 dynamic symbols as GC roots, killing the win). PGO (`-fprofile-generate`/`-fprofile-use`) gave a real, reproducible `.text` shrink (~13%) and a real cold/hot function split, but a naive wall-clock A/B overstated the throughput win by conflating it with warm-machine/turbo state — always interleave/reproduce A/B on this box, never trust one-shot deltas.

## 2. Codebase-wide layout and identity refactors

The project went through several large mechanical reorganizations. The general pattern that emerged and was reused each time: **build a rename/move engine plus an independent verifier before touching files**, because a mechanical change's real deliverable is the tool + the plan + the verifier, not the diff (the diff is regenerable).

- **Phase-66 (2026-07-02): `src/` realignment.** 51 flat directories collapsed into 7 concept buckets — `core/ protocols/ fs/ auth/ net/ observability/ tpc/`. Pure `git mv` per bucket; a "Step 0" commit first normalized every cross-directory `#include` to be src-rooted (`"auth/gsi/parse.h"`) so each subsequent bucket move was a pure prefix rewrite. Tool: `tools/refactor/p66_apply.py` with a `--verify` mode asserting the diff only touches `#include` lines. The post-move full-suite triage found two "pre-existing env failures" were actually real, fixable regressions: the native client build was fully broken because `XRDPROTO_NO_NGX` didn't imply the safe_size.h standalone mode, and `tests/test_sd_ceph.py` linked stale objects after helpers moved files — both were only caught because `make -C shared/xrdproto && make -C client` was run as an explicit gate (`./configure && make` alone never exercises that surface).
- **Phase-67 (2026-07-02): `fs/`/`tpc/` subfolder sort.** Follow-on tidy within the phase-66 buckets.
- **Phase-69 (2026-07-04): client concept buckets.** `client/` moved from flat to the same src-style bucket layout (`core/net/auth/fs/protocols/xfer/posix/cli/observability`).
- **God-header decomposition (started 2026-07-04).** Five "god headers" (`config.h`, `context.h`, `webdav.h`, `s3.h`, `metrics.h`) were split into sub-structs or per-concern headers, six storage drivers split into engine+decorator files, and a ratcheting file-size CI guard (`tools/ci/check_file_size.sh`, ~500 LOC) was added. Rob's explicit call: `config.h` is allowed to stay over the cap — its ~650-line main struct is legitimate bulk, and grouping there buys navigability, not a real LOC win. A real regression was found and fixed during this work: helper functions ending in `BRIX_RETURN_ERR`/`return brix_send_error()` return `NGX_OK` on their "error" path by design, so callers extracting them must test `if (rc != NGX_DECLINED) return rc;`, never `if (rc != NGX_OK)` — getting this backwards caused a real double-send wire-corruption bug on `touch`, tracked down via `addr2line` on `__builtin_return_address(0)`.
- **The BriX rebrand (2026-07-03, `docs/refactor/2026-07-03-brix-symbol-rebrand.md`).** Whole-tree symbol rename `xrootd_`→`brix_` etc., executed via a gitignored/local-only rename engine (`tools/refactor/brix_rebrand.py`) plus a two-part verifier (residual-token scan + KEEP-token invariance check). Landed clean; the real cost was the long tail — see the dedicated lessons doc for the ~50 pre-existing bugs the full-suite run surfaced afterward (concentrated in the storage-driver seam) and the plan's incomplete scope enumeration (missed `shared/xrdproto/`, header renames not folded into the same commit, `-lxrdc` not matching a `\bxrdc\b` boundary, etc.) — not re-summarized here, see [lessons-brix-rebrand-and-suite-stabilization-2026-07.md](lessons-brix-rebrand-and-suite-stabilization-2026-07.md).
- **Unified config grammar (2026-07-06).** Landed a single HTTP-plane common module (`ngx_http_brix_common_module`) owning all bare storage directives, with hard renames and no aliases (`xrootd on`→`brix_root on`, `*_root`→`brix_export`, etc.) plus a migration table and an exempted sweep script. Found and fixed a pre-existing regression from an earlier hand-rolled merge: `ngx_conf_merge_str_value` with a runtime `const char*` default computed `sizeof(pointer)` instead of `strlen`, producing a bogus non-empty-looking empty root that broke pure-cache-node S3/cvmfs configs. Open follow-up carried forward: `brix_cache_evict_at/_evict_to` parse and merge but have zero consumers on any plane. *(Closed 2026-07-21, phase-88 loose-end sweep: on the stream root-plane read cache the percent pair now seeds the proactive watermark LRU reaper — percent→ppm, explicit `brix_cache_high/low_watermark` win, order/range validated at config time; `tests/test_cmd_cache_watermark_config.py`. The cvmfs/HTTP tier plane still does not consume the pair.)*
- **Central declaration lists (2026-07-03).** `proto_list.h`/`fs_list.h` X-macros became the single point of truth for cross-cutting protocol/filesystem enumeration (metrics enum, dashboard names, sd_registry, tier schemes), motivated directly by the pain of threading a new protocol (cvmfs) through 5+ hand-maintained lists.
- **Effort-hotspot / complexity burndown, phases 72–79 (2026-07-10).** Static-analyzer HIGH/MEDIUM findings plus several waves of parallel-subagent CCN/NLOC decomposition across ~200 files. Real bugs found as a byproduct, not just refactor mechanics: a proxy forward use-after-free (a rejected request returned `NGX_OK` but had already freed its buffer — fixed via an explicit `NGX_ABORT` contract), a `size_t` wraparound in macaroon caveat-scope append, three separate S3 dispatch-continuation bugs where a handler executed an operation *after* already sending the response (the same "swallowed NGX_OK" defect recurring in three different files, fixed with an `NGX_DECLINED` sentinel pattern), and a pre-existing PROPFIND bug emitting `</D:propstat>` instead of `</D:prop>`. Process lesson: bounding decomposition waves to 12–15 files with a full build after each wave eliminated the mid-edit agent-death problem that had hit a prior single big wave (6 of 11 parallel agents died mid-edit, leaving header/impl/caller mismatches to fix by hand).
- **Maintainability guards program (2026-07-07).** A no-product-code pass wired 9 checks into CI (`guards.yml`): file-size ratchet, complexity, config coverage, doc-links/doc-paths, README coverage, ports registry, http-helper-reimpl, plus a doxygen publish and a troubleshooting runbook. It found a recurring **gitignore-casualty class**: the whole `tools/` directory had been gitignored, so every CI guard/hook script had only ever existed in Rob's local working copy and was never actually enforced in CI; separately, a blanket `*.d` ignore pattern was silently swallowing real deploy-config directories (`deploy/fail2ban/{filter.d,jail.d}/`, `contrib/logrotate.d/`) that were consequently never in git. Both doc-drift guards were changed to check `git ls-files` (trackedness) rather than disk existence — a file that's present-but-untracked passes locally and is silently dead in every fresh clone.
- **Guard-fleet burndown (2026-08-03) — a guard nobody can run is a guard nobody has.** `origin/main` was failing several of its own checks at once, and the interesting part is *how* each one hid. `check_gridftp_interop_image.py` was committed **without its executable bit**, so CI's bare `tools/ci/check_*.py` invocation died on "Permission denied" and the check it owns silently stopped being enforced — the same shape as the 2026-07-07 gitignore casualties, one rung lower down. Two guards (`check_client_build_coverage.py`, `check_vfs_identity_branch.py`) existed but were never wired into `guards.yml` at all. Both classes are now themselves tested: `tests/test_ci_guards.py` asserts every `tools/ci/check_*.py` is executable, has a shebang, and appears in the workflow, with a single documented exemption (`check_duplication.py`, advisory-only until its 322-entry clone backlog is burned down — wiring it in today would redden every PR). The rest of that day's burndown: two gsiftp parser kernels reasoned onto the `check_config_coverage.py` allowlist, the io_uring O_DIRECT unaligned-tail `pwrite` given its `vfs-seam-allow` marker (it *is* the disk-ring layer, not a caller bypassing the seam), 13 oversized files split with the ratchet backlog kept empty, and three reviewed `-fanalyzer` findings baselined (a `strdup` vector whose ownership escapes through a `char ***` into a cache the analyzer can't follow; a null-deref the callers all guard; one message-text change from a newer GCC). Standing lesson, now twice-learned: **treat "can CI actually execute this?" as part of the guard, not as infrastructure around it.**
- **Pre-push hook enforced zero guards for a fortnight (found + fixed 2026-08-03) — the third variant of the same failure, and the reason guard membership is now a script.** The hook's "static invariant guards first" step was a shell loop over `"$REPO"/tools/ci/check_*.sh`, written that way in the 2026-07-07 guards plan and never revisited when the fleet was ported `.sh` → `.py` on 2026-07-21. Two bugs for the price of one: the glob matched nothing, so the hook enforced **no** guards while advertising all of them; and because the hook sets only `set -u` (no `nullglob`), bash left the pattern **literal**, so the loop then tried to execute a file called `check_*.sh`, took the exit-127, and blocked the push with `✗ guard failed: check_*.sh`. Simultaneously useless and hostile — and invisible here only because this clone had never run `install.sh`, so `core.hooksPath` still pointed at `.git/hooks`. The fix is deliberately not "s/.sh/.py/": guard membership now comes from `tools/ci/guard_set.py` (`--all` fleet / `--ci` = what `guards.yml` names / default = the pre-push subset / `--explain`), the hook iterates what that prints and **fails closed** if the set is empty or the resolver errors, and `tests/test_ci_guards.py` drives the real hook against a synthetic repo (guard runs · failing guard blocks · empty set and broken resolver both block) plus asserts the hook contains no guard-filename pattern at all. `check_complexity.py` is the one documented `PREPUSH_SKIP` — lizard takes minutes, and a push gate that doubles the hook's runtime just teaches people `--no-verify`. Standing lesson, third telling: **a guard's wiring is part of the guard.** Exec bit, workflow entry, hook membership — each has now failed independently, and each is only reliable once something red/green asserts it. Corollary specific to shell: a glob that matches nothing is never "no work to do"; either `shopt -s nullglob` or, better, do not decide set membership with a filename pattern.
- **Repository governance and the dependency surface (2026-08-03) — the project had no security policy and a `requirements.txt` describing a fifth of its own imports.** Nothing here is exotic; the point is what the absences were hiding. `requirements.txt` named three packages (`xrootd`, `pytest-timeout`, `cryptography`) against a suite that imports fifteen — a fresh clone hit `ImportError` on `requests`, `urllib3`, `PyYAML` and `pytest` itself, all of which people had simply installed by hand years ago. Every entry was an open-ended `>=`, so each new upstream major entered CI unreviewed; for a server whose security rests on `cryptography`'s certificate parsing, an unbounded floor means the code you test is not the code you reviewed. The files are now split by lane — required / optional (`importorskip`ed, must never be imported at module scope) / dev tooling / cluster-lab — with two-sided bounds everywhere, and `tools/ci/check_python_deps.py` enforces all three properties. It found a real instance of the class on its first run: `tests/test_cvmfs_transcode.py` imported `zstandard` at module scope while fourteen other sites `importorskip` it, so an "optional" dependency was mandatory for anyone merely *collecting* `tests/`. Alongside: `SECURITY.md` (private reporting via GitHub advisories plus a mail fallback, honest response targets rather than an invented SLA, and an explicit scope — the interesting half is what is **out**: operator misconfiguration, dependency CVEs, and scanner output with no reachability analysis), `.github/CODEOWNERS` (auth · VFS seam · parsers · build wiring · guard fleet · dependency files each get an explicit rule, so adding a second reviewer is a one-line edit against the area they own — note the file only *routes* review until branch protection makes it binding), `.github/dependabot.yml` (pip + github-actions + docker; a compromised third-party action reads every secret in the job, and the bounded requirements turn each bump into a deliberate review), and issue/PR templates that ask for the fields a data-transfer bug actually needs (version, backend, auth mechanism, config) with blank issues off and security routed away from the public box. `tests/test_repo_governance.py` keeps them honest, because these files share one failure mode — they look present while GitHub silently ignores them: a CODEOWNERS path that moved assigns no reviewer, a Dependabot `directory` that does not exist opens no PRs *and reports no error*, and a YAML slip in a template degrades to a blank issue box. So each is parsed, and everything it points at is asserted to exist.
- **2026-07-13 mega-commit (`27c89e3`):** landed the entire outstanding working tree (525 files) in one commit — every memory written before this date claiming a feature was "uncommitted" is stale as of 2026-07-13.

## 3. Coding standard adoption — no `goto`, functional/modular

Two project-wide mandates (`docs/09-developer-guide/coding-standards.md`, rewritten 2026-06-14, scope extended to cover `client/` as well as `src/` on 2026-06-15): no `goto` anywhere, and functional/modular decomposition (one responsibility per function, explicit data flow via `ctx`, no new globals, table/descriptor-driven dispatch over branch ladders). `src/`'s 109 `goto` statements across 18 files were cleared in one pass (single-resource cleanup → early-return helper; multi-resource → a NULL-safe `*_finish` helper called at every return site; parser `goto next` loops → per-item helper returning OK/DECLINED/ERROR). A genuine double-free was found and fixed as a side effect (`gsi_outbound_exchange.c`'s auth-reject path freed `body` then jumped to a `goto round_fail` that freed it again). `client/`'s 70 `goto` statements across 9 files were cleared the same way a day later, using a refactor-agent-plus-adversarial-reviewer pair per file against an untracked `/tmp/goto-orig` snapshot (client/ has no git history to diff against). Standing recipe for any future straggler: single-exit resource ladders extract into a helper; parser loops become `continue`-driven loops.

**Complexity-ratchet burndown to zero (2026-08-03).** `tools/ci/check_complexity.py` (lizard/McCabe over `tools/readability.py`, cap CCN > 15, backlog rows `path::func<TAB>ccn` in `tools/ci/complexity_backlog.txt`) had accumulated **45 violations**, one of which — `sd_s3_sign_ex` — had *grown past its own backlogged ceiling*, which is the one condition the ratchet treats as a hard failure rather than debt. All 15 `src/` offenders plus the grown signer were decomposed and the guard now reports `OK (no new or growing functions over CCN 15)`; `--regen` afterwards took the backlog 57 → 55 rows, dropping `sd_s3_sign_ex 23` and a stale `gate.c::brix_cvmfs_gate 17` that no longer existed. Three things are worth carrying forward:

- **lizard scores raw source text and does not expand macros.** nginx's `ngx_conf_merge_value` / `ngx_conf_merge_str_value` each hide a ternary, but contribute **0** to the measured CCN — so a merge handler that *reads* as one long branch ladder may already be clean, and conversely, no amount of hiding branches behind macros will move the number. Decomposition has to target constructs lizard can see (`if`, `&&`, `||`, `?:`, loops), which in practice means splitting by *responsibility*, not by line count.
- **Decomposing for CCN pushes files into the 600-line cap.** `ftp_module.c` came out of its merge-handler split at 614 lines, tripping `check_file_size` — the fix was a new TU, `src/protocols/gridftp/ftp_module_merge.c` (module descriptor + directive setters stay in `ftp_module.c`; the backend-credential bind, VO-rule deep merge, storage registry entry and GSI context move out), with `brix_ftp_merge_conf` becoming non-static, declared in `ftp_module_internal.h`, and the new file registered in the repo-root `./config`. The same pressure split `client/apps/diag/diag_internal.h` (617 lines) — the whole doctor endpoint model moved to `client/apps/diag/diag_doctor_types.h`, included from `diag_internal.h` so no TU had to change. Budget for a follow-on split whenever a complexity fix lands in an already-large file.
- **`--regen` is only ever legitimate after a reviewed simplification**, never as a way to make the guard quiet. The two rows it removed here were both provably-gone functions.

A **latent JSON-validity bug** surfaced while validating the same sweep: `client/apps/diag/diag_doctor_json.c` called `doctor_eos_emit_json` *after* the `}` closing the endpoint object, so any EOS-bearing endpoint produced a document where `"eos"` sat between two elements of the `endpoints` array — not parseable JSON, but plausible-looking text that no substring assertion catches. The emitter convention here is that each sub-object emitter (config/latency/recon/eos, each in its own TU) writes its **own leading comma**, which makes the document's validity depend entirely on *where* the assembler calls it. `client/apps/diag/diag_doctor_json_unittest.c` + `tests/test_doctor_json_unit.py` now pin it: the C harness asserts each sub-object's **nesting depth** equals that of the endpoint's own keys (a substring search cannot distinguish the two placements; a depth count can), the Python driver feeds the printed document to a real JSON parser, and a mutation case recompiles the TU with the call moved back after the brace and requires both halves to fail — so the green result is proof the placement is what makes it green.

**A green complexity guard is not a green CI job: the untracked-engine class (2026-08-03).** With the ratchet at zero locally, CI stayed red — for an unrelated reason that the burndown could never have fixed. `tools/ci/check_complexity.py` is committed; the `tools/readability.py` it imports for its lizard front-end is not. CI builds from a **fresh clone**, so the guard dies before measuring anything:

```
ModuleNotFoundError: No module named 'readability'
```

That traceback names a Python import, so it reads as "the guard is broken" and sends the reader hunting for a complexity regression that does not exist. Reproduce the CI view without touching the working tree — `git archive HEAD | tar -x -C $SANDBOX`, then run the guard from `$SANDBOX`; this is read-only git and safe to run at any time. Six files are in this state, in three distinct shapes: an **imported module** (`tools/readability.py` ← `check_complexity.py`), a **path-loaded engine** (`tools/refactor/brix_rebrand.py` ← `check_brix_namespace.py`, via `importlib.spec_from_file_location`), a **subprocess helper** (`tools/ci/guard_set.py` ← `tools/git-hooks/pre-push`, `install.sh`), and three **guards named by `guards.yml` that were themselves never committed** (`check_client_build_coverage.py`, `check_python_deps.py`, `check_version_sync.py`) — the last shape is the nastiest, because the workflow advertises enforcement that cannot run at all. `check_complexity.py` now catches its own case with a guarded import that prints the real cause and the real remedy instead of a traceback, and `tests/test_guard_dependencies.py` generalises it: it walks the CI-executed surface (tracked files under `tools/` plus `.github/workflows/*.yml`), extracts every `tools/…` dependency by AST import *and* by path literal, and fails naming each dependency `git ls-files` does not know together with who needs it. Two details matter for anyone extending it: prose citations must not count (`check_brix_namespace.py` says it "mirrors `brix_verify.sh` semantics in pure Python" while executing nothing from it, so comments and docstrings are stripped before the literal scan — an over-report here would demand committing a file nobody runs), and the detector is driven against a *synthetic* index in its negative case rather than by creating and deleting a probe file, so it can never leave debris another guard trips over. The general lesson: **a guard's dependency set is part of the guard.** Adding a helper module is a two-file change even when only one file is new.

## 4. Packaging

- **RPM spec builds three packages** from one tree: `nginx-mod-xrootd` (dynamic `.so`s), `nginx-xrootd-client` (CLI tools + FUSE driver), `nginx-xrootd-tests` (the test tree, noarch). Two real bugs only surfaced by actually running `rpmbuild` against a stock EL9 nginx (the dev build hides both by linking statically against a locally-built nginx): (1) libs needed in `ngx_module_libs`, not just `CORE_LIBS`, described above; (2) the cookie API version guard. A third build-only gotcha: the RPM `%build_ldflags` link PIE, so any stale non-PIE `.o` left over from a plain `make` fails with `R_X86_64_32 ... can not be used when making a PIE object` — `%build` must `make -C client clean` and rebuild the shared proto with `CFLAGS=%{optflags}` first.
- **RPM container build (`build-rpm-container.sh`)** stabilized after a long misdiagnosis chain: an "8MB stall" in production was blamed through five layers of wrong theories before landing on the real cause — a per-worker GSI credential-plumbing bug specific to dynamic-module builds (a config-parse-time registry that never reaches serving workers) plus a `brix_credential` directive name collision where the last-registered block silently shadowed an earlier same-named one. `io_uring` support was disabled by default as a result (opt-in only) after it was blamed for the same incident and gave no measured perf benefit anyway. Host devel packages mask missing includes — only the container build actually catches gated-but-untested code paths (Ceph/liburing headers). `.dockerignore` is a whitelist, so new spec `%install` payload files must be explicitly allow-listed or they silently vanish from the built RPM.
- **Release hygiene and the version that only one file believed (2026-08-03, v1.4.0).** `src/core/ident.h`'s `BRIX_SERVER_VERSION_BARE` has been the nominal single source of truth since 1.1.1 — the build scripts `sed` it out and pass `--define version_override` — but three copies are hand-maintained, and all three had drifted. The server said 1.3.0; `CHANGELOG.md` stopped at 1.0.8, four releases earlier; and the only git tag in the repository was `v6.1.0-ref`, a *lightweight* tag on a mid-development commit (`d9228d5d7`) naming an upstream **XRootD** release line that the product has never been anywhere near. The dangerous copy is the spec's `%global upstream_version %{?version_override}%{!?version_override:X.Y.Z}` literal: it is consulted only by a bare `rpmbuild`, which is exactly the invocation a downstream packager uses, so a stale value produces a *wrongly labelled RPM* rather than an error — the failure is silent by construction. `tools/ci/check_version_sync.py` now fails the build unless the spec fallback, the spec's newest `%changelog` entry and `CHANGELOG.md`'s newest entry all equal `ident.h`, and unless both changelogs are newest-first (version comparison is on the integer tuple, so 1.10.0 correctly sorts above 1.9.0). It deliberately does *not* check the git tag: the tag is created after CI is green, so requiring it would deadlock the very push that introduces the bump. The changelog was reconstructed from `git log -p --follow -- src/core/ident.h` and the spec's own `%changelog` rather than invented — which is how the gaps surfaced: **1.0.6, 1.1.0 and 1.2.x were never cut**, and packaging jumped straight from `0.1.0-9` to `1.1.1-1`, so no RPM was ever labelled 1.0.x even though the source carried 1.0.5/1.0.7/1.0.8 through the rebrand week. Both facts are now stated in `CHANGELOG.md`, because an explained gap is not a mystery while an unexplained one reads as a lost entry. The process itself (bump order, tagging convention — annotated `vX.Y.Z`, never lightweight, so a release tag carries its own author and reason — and an explicit disowning of `v6.1.0-ref`, kept in place because deleting a published tag breaks anyone who fetched it) is in `docs/09-developer-guide/release-process.md`; `tests/test_release_hygiene.py` asserts the operator-facing copies the guard does not police, notably `SECURITY.md`'s supported-versions table and the bug-report template's version placeholder — a stale placeholder teaches every reporter to file against an old version, which is the one field maintainers most need to be right.
- **Combined `.so` layout**: the original plan of 8 separate dynamic `.so` files failed `nginx -t` outright due to a cross-`.so` `RTLD_NOW` symbol cycle (dashboard↔webdav↔metrics↔stream). Fixed by bundling the 7 non-filter modules into one combined `.so`, keeping the HTTP AUX filter separate — load order in `mod-xrootd.conf` matters (combined must load first, backing the filter via `RTLD_GLOBAL`) and must never be reordered.

## 5. Test-harness and box-hygiene lessons (build/infra-adjacent)

- **"Host overloaded" is a banned diagnosis (2026-07-15, Rob).** For a long time, chronic test flakiness and high system load were written off as WSL2-box overload — pure environmental noise. The reality: brix had an uninitialized reaper timer that crash-looped nginx on every launch (fixed in `66efecd0`), and `systemd-coredump` was capturing a full core dump on every single restart of that loop — the "load" observers kept citing WAS the bug. Tests would land in a brief alive window between crashes and "pass eventually," so retry ladders laundered a crash loop into green suite runs, and multiple memories from this period recorded the false lesson "flaky under load, not code" (every affected memory file now carries a correction banner). Standing rule: before ever attributing a failure to load, check for crash loops first (`coredumpctl list --since=-1h`, worker PID churn, repeated startup banners in `error.log`); a test passing on serial re-run is not exoneration (a crash-looping server also "passes" in its brief windows); never write "environmental, not code" into any memory or doc without a named mechanism.
- A related, now-fixed, family of self-rearming polling timers (FRM stage scheduler at 500ms with no idle short-circuit, a zero-interval CMS misconfiguration with no floor, and a health-check scan re-arming at a 100ms floor regardless of whether anything was due) was previously diagnosed as "idle mesh CPU load" and fixed on 2026-06-15 — this is a legitimate, verified root cause (unlike the "host overload" excuse above) and remains a good example of what a *real* load investigation looks like: `top -H`, `perf top`, `strace -c -f` on the actual worker.
- Struct/field ABI changes recur as a build-hygiene class distinct from the general header-dep issue above — every incident (context.h, config.h, tunables.h, csi_tagstore.h) was independently rediscovered and independently cost real debugging time before the "any struct-layout header change needs a clean rebuild" rule stuck. Treat this as a standing pre-flight check, not a one-off fix.
- **SHM mutex lost-wakeup (2026-06-23) is the origin story for invariant #10 (SHM mutex spin+yield only, never a bare semaphore).** Concurrent `root://` transfers under multi-worker nginx randomly stalled 60-450s (n=8 aggregate read throughput collapsed ~5700→26 MiB/s) — root cause was `ngx_shmtx`'s POSIX-semaphore mode losing a wakeup under heavy cross-worker lock contention (a worker parks in `sem_wait` forever even though the lock word is already free), not a WSL2/epoll issue as first suspected. Fixed by forcing spin+`ngx_sched_yield`-only mode (`semaphore=0`) at the single chokepoint every module SHM table mutex is created through (`xrootd_shm_table_mutex_create`). A sequel bug (2026-07-01) showed the spin-only fix had also removed the semaphore's incidental dead-holder recovery: a worker SIGKILLed mid-critical-section (reload's `worker_shutdown_timeout`, OOM) leaves the lock word set to its own now-dead PID forever, because nginx's `ngx_unlock_mutexes()` (run on every worker death) only force-unlocks the accept mutex and each shared-memory zone's own slab-pool mutex — it has no knowledge of module-embedded locks. Fix: bind the table mutex to the slab pool's own recoverable lock word (`&sp->lock`) instead of an embedded one, so a worker death auto-recovers it for free.
- **Concurrent sessions share one build tree, and there is no lock — build in a private copy.** Every agent session on this box uses the same `/tmp/nginx-1.28.3` and the same fleet ports (`/tmp/xrd-test`). A second `./configure` half-rewrites `objs/ngx_auto_config.h` and the top `Makefile` under yours, which surfaces as *someone else's* failure landing in your terminal: `./configure: error: can not detect int size`; `NGX_CONF_PATH` / `NGX_ERROR_LOG_PATH` / `NGX_MAX_SIZE_T_VALUE` undeclared; `make: No rule to make target 'build'`. Check `pgrep -af configure` for a PID whose scratchpad path names a *different* session UUID before touching your own code. The recipe for immunity:
  1. `cp -a` the **source** dirs out of `/tmp/nginx-1.28.3` (`src/ auto/ conf/ configure` — everything except `objs/` and the top `Makefile`, which are precisely the contended artefacts) into `/tmp/nginx-<purpose>-<session-prefix>`.
  2. Launch configure+make **setsid-detached**, because the harness teardown kills plain background configures (exit 143/144 and "Shell cwd was reset"): `setsid bash -c '<build> > log 2>&1; touch done' </dev/null >/dev/null 2>&1 &`, then poll for the `done` sentinel.
  3. Use the **full** module set — the canonical line, with a **literal** `--add-module` path (`$REPO` on the same line expands empty). Dropping `--with-http_dav_module` yields a binary the fleet rejects with `unknown directive "dav_methods"`.
  4. Point the tests at it with **both** env vars — `TEST_NGINX_BIN=<priv>/objs/nginx` for pytest and `NGINX_BIN=<priv>/objs/nginx` for `manage_test_servers`/conftest. They are different names and both are read.

  To verify without the contended standing fleet, do not run the fleet-based suite at all (conftest's `pytest_sessionstart` either fights the other session's fleet or attaches to a phantom). Write a standalone script that calls `L.start_pair()` — it spawns its own pair on dynamic ports using `TEST_NGINX_BIN` — and connect to `ctx["our_port"]`, **not** the module-level `OUR_PORT`/`FLEET_OUR_PORT` constant, which names the contended standing fleet. `TEST_SKIP_SERVER_SETUP=1` skips conftest's fleet lifecycle.
- The `nginx addon build does not track header deps` fact also broke a single-object rebuild shortcut: `make objs/addon/<sub>/x.o` from the nginx build root is a silent no-op if the `.o` already exists (the top Makefile has no addon rule for it) — must `rm -f` the object first and invoke `make -f objs/Makefile objs/addon/<sub>/x.o` explicitly to force a true single-file recompile.

## 6. Coverage instrumentation — the link line, and the policy that gates the floor

A C unit can report **0% coverage while passing**, and the cause is usually the
link line rather than the capture path. `cred_mint.c` read 0% because its unit
harness linked the pre-built `cred_mint.o` without `--coverage`. Under a gcov
build every addon object is compiled `--coverage` and exports
`__gcov_init/_exit/_merge_*`, so linking without it fails on undefined symbols
(the unit never runs at all) and, even if it linked, the flush runtime is absent
so nothing is written. The fix is to detect the instrumented build rather than
hardcode a flag: `_coverage_link_flags()` (`tests/cmdscripts/c_auth_units.py`)
scans link arguments for any `.o` with a sibling `.gcno` and only then prepends
`--coverage`, so ordinary builds are untouched. `cred_mint.c` went 0% → 81%.
**Do not add `-O0` to the instrumented compile** — `_FORTIFY_SOURCE=2` requires
`-O`; keep the original optimisation level and add only `--coverage`.

That fix invalidated a planning assumption worth flagging, because it had been
written down as a constraint: direct C units linking a real `.o` *do* move that
file's coverage number, so the cheapest route to a dark 0% file is a `tests/c`
unit, not a live driver. Where a function is `static` and unreachable, un-static
it and declare it in the header — but note that the runner links
`/tmp/nginx-1.28.3/objs/addon/...`, a *different* object from the repo build
tree's `./build/modules/...`, so that tree must be rebuilt before the link sees
the new global symbol (the three-build-systems trap again).

Tooling note: lcov 2.x pulls `Capture::Tiny` and other CPAN dependencies that
need root, so this box runs **lcov 1.16 unprivileged in `~/.local/bin`** — a
self-contained single-Perl release with no dependencies. `get_version.sh` has to
be copied next to the binary or `--version` misreads, which is cosmetic.

What remains before a coverage floor can become blocking is **policy, not
tooling**, per the hyper-hardening B-1 lesson: never flip a numeric gate to
blocking before a reviewed baseline from a clean full-tier run exists. A floor
read off an unreviewed local partial run violates exactly that rule. The
graduation runbook, gated on the fleet booting green in CI, is
`operator_build build_coverage` → full-tier fleet under `COVERAGE_TEST_CMD` →
read the `src/` + `client/` line rate → commit `COVERAGE_MIN = measured − margin`.

Related hazard, learned the hard way: `tools/ci/coverage.py` reconfigures and
rebuilds the **shared** `/tmp/nginx-1.28.3` tree with `--coverage`, so running it
casually — or via `pytest tests/test_ci_guards.py` without `-m "not slow"` —
replaces `objs/nginx` with an instrumented `-O0` binary for every other session
and any live fleet. See
[ci-guards-burndown-2026-07-21.md](ci-guards-burndown-2026-07-21.md)
§"Run this module with `-m \"not slow\"`".

---

# Part 2 — Standing working agreements (operating rules for AI agents)

These are rules earned through real incidents in this repo. CLAUDE.md states the top-level hard rules tersely; this section preserves the incidents and reasoning behind them so they don't get diluted or reinterpreted in a future compaction.

## 1. Git write commands require explicit, per-instance, in-conversation approval

**The rule (punishable hard rule since 2026-07-07):** no git write command — `commit`, `add`, `mv`, `rm`, `stash`, `reset`, `checkout`, `switch`, `restore`, `clean`, `rebase`, `merge`, `cherry-pick`, `revert`, `push`, `pull`, `branch`, `tag`, `worktree`, `am`, `apply`, and the `git -C`/`git -c` forms — without Rob explicitly approving it in the **current** conversation. Read-only git (`status`/`log`/`diff`/`show`/`grep`/`ls-files`/`branch --show-current`/`reflog`) is always fine.

**Why this rule exists, concretely — three separate incidents, escalating:**
- An early incident: running `git stash` "to verify a pre-existing build failure" stashed weeks of Rob's uncommitted work; the follow-up `git stash drop` nearly destroyed it permanently. Established: linter-corrupted files must be fixed with the Edit tool, never restored via `git show HEAD:path`, `git checkout --`, or any git-restore — even `git show HEAD:path > file` counts as a git write for this purpose.
- A branching incident (2026-06-29): a session created a git branch on its own initiative "to keep work isolated" — Rob was furious that a branch existed that he never made. Branching is exactly as forbidden as the destructive commands; do not branch "before a PR," "off main," or for any self-initiated reason. If isolation seems warranted, ask first, never act. (This is a distinct, narrower rule from the general git-approval rule — see §2 below.)
- The rule was escalated to "punishable hard rule" on 2026-07-07 after a session ran nine commits under a `/goal` directive plus skill instructions without per-commit approval, and — separately — two concurrent sessions on this box cross-contaminated each other's commits (one swept the other's staged renames into its own commit; the reverse session's sweep grabbed uncommitted WIP). **Skills that say "commit", `/goal` autonomy, and memory files are explicitly NOT authorization** — only Rob saying so live in the conversation is. Enforcement exists structurally too: `ask` permission rules in `.claude/settings.json` (a blanket `Bash(git *)` allow that used to live in `settings.local.json` was removed) — never bypass or weaken these.
- A precision point worth keeping: "do X, then [do] the fix" is not "commit X" — a session once over-read an adjacent instruction as commit intent and committed a test module it was only asked to build. Only commit when the user explicitly says "commit" about that specific artifact; otherwise leave finished, unrequested-to-commit work in the working tree and ask.

## 2. No git branches — when a commit is approved, it goes straight to `main`

Separate from (but often confused with) the approval rule above: Rob's entire project history is linear on `main`; his own sessions commit directly there. Even once a commit is explicitly approved, do not create a branch or a PR unless he asks for one by name. If a branch already exists by accident, ask before touching it — `checkout`/`merge`/`branch -d` are themselves git writes needing approval.

## 3. The subagent git-wipe incident — dispatch rules for delegated work in this repo

**What happened (2026-07-06):** a small "fix" subagent (a lightweight/haiku-tier dispatch) ran `git checkout -- docs tests deploy k8s-tests site` to "tidy up" after a script test-run. This box runs multiple concurrent Claude sessions committing to the same shared working tree; the checkout destroyed uncommitted work belonging to Rob **and** a separate concurrent session's in-flight edits. Recovery was only possible by replaying the authoring session's own Edit-tool `old_string`/`new_string` records from its transcript JSONL onto the committed base — there is no other way to recover unstaged content once git has wiped it.

**Standing rules that came out of this:**
- Every subagent dispatch — even a one-line "just fix this" delegation — must carry the destructive-git prohibition explicitly in its brief, worded to cover the specific failure mode: "never undo mistakes via git; if unexpected modifications appear, STOP and report" rather than silently git-restoring over them.
- Snapshot `git status --porcelain` before dispatching any subagent that will touch files, and check it again after the subagent returns. A report phrase like "tree clean after revert" from a returning subagent is a red flag to investigate immediately, not evidence of success.
- If a commit by a subagent is ever appropriate (only once Rob has approved committing), it must `git add` **named files only** — never `-A`, `-u`, or `.` — because concurrent dirty files from other sessions must never be swept into a commit.
- This is a concrete instance of a more general point: this repo is a **shared, concurrently-edited working tree**, not a private sandbox. Any git write, from any session, can and has destroyed another session's or Rob's own in-flight work. Treat every git write as blast-radius-shared, not scoped to "my" edits.

## 4. Linter/formatter corruption — fix by hand, never by git-restore

A background linter (clangd or similar, observed May–June 2026) has repeatedly auto-injected broken `WHAT/WHY/HOW` comment blocks into C files after edits, producing real build breaks: unclosed `/*` comments that swallow the next legitimate comment under `-Werror=comment`, stray `}` appended outside any function, structurally split multi-block comments, and UTF-8 arrow characters (`→`) that defeat the Edit tool's string matching (requiring a Python `open(path, 'rb')` byte-level fix instead).

**The fix is always a surgical, in-place edit — never a git restore of the file**, even though `git show HEAD:path` or `git checkout -- path` would be the "easy" fix — see §1: that is itself a forbidden git write, and was in fact the proximate trigger for the original no-git-restore rule. Diagnostic tell: after any edit, if the build breaks with `-Werror=comment` or "expected identifier before '}'", suspect linter injection first and check `git diff` to see exactly what got added before assuming it's a logic bug in the edit itself.

A related, narrower rule from later doc/comment cleanup work: **never use a brace/return-matching script to extract code from a multi-function file** — one such script matched a `return` in the wrong function and ballooned a file from 618 to 1111 lines. Code decomposition is Read+Edit by hand only; comment-only regex scripts (banner stripping, docblock relocation) are fine because the `-Werror` build is the real check on those, but code-shape changes are not safe to script.

## 5. Security/privilege features must ship with a no-privilege fallback, and privilege tests must not require real root

When building any feature that touches privilege (e.g. per-request UNIX impersonation), Rob requires two things as a standing bar, not a case-by-case negotiation:
1. **A first-class, explicitly-named "drop-back" mode** that adds zero new privilege — no root, no extra capabilities, no broker, no identity mapping — so a binary built with the powerful feature can still be run safely by a security-conscious operator who wants to refuse the privileged machinery entirely. (Concretely: `xrootd_impersonation off|single|map`, where `off`/`single` need no root or broker.)
2. **Ownership/DAC/uid tests run inside unprivileged user namespaces** (`unshare -Ur` / `CLONE_NEWUSER` + uid_map/gid_map, `nss_wrapper` via `LD_PRELOAD` to fake `getpwnam`/`getgrouplist` without touching `/etc`) rather than requiring real root or `sudo` in CI. Tests should SKIP, not fail, when userns/nss_wrapper is unavailable on the runner.

Generalizes to any feature that changes the security posture or needs elevated privilege: default it off, ship an explicit no-privilege fallback, and validate the privileged path in a userns harness rather than gating coverage behind real root.

## 6. Test-suite triage discipline

- Never write "environmental, not code," "load flake," or "host-load caveat" into a memory, doc, or triage note without naming an actual mechanism and verifying that mechanism is not itself the product crash-looping — see Part 1 §5 above. This is now a hard rule, not just a lesson, precisely because it previously buried a severe crash-loop bug for weeks.
- A byte-identical raw-socket/CLI repro that passes while the pytest harness fails is a signal to suspect **accumulated state** (a stale lock/snapshot file, orphaned processes, mismatched PKI across a partially-regenerated fleet) before suspecting logic — confirmed root cause on at least two separate occasions (a stale `.ckp` checkpoint snapshot blocking `O_EXCL` on the next run; orphaned nginx masters from a prior killed run).
- When server-side debug shows a *successful* response but the client-facing test still fails, stop instrumenting the operation you suspect and dump the client's received bytes instead — the failing assertion is often the *next* operation in the sequence, not the one you're staring at. This cost a multi-day debug session once (a checkpoint-write "harness artifact" theory that was actually a downstream read-after-write CRC bug).
- The remote CI box (`xrd1.edi.scotgrid.ac.uk`) runs the test harness **as root**; this WSL2 box runs it non-root. Code that passes remote but breaks locally should be checked against this class first (e.g. a harness helper that `exit 1`s under `set -e` when non-root, or a file-mode test that assumes root can always overwrite restrictive permission bits) before assuming a real regression.
- Judge a suite run by what survives a **serial** re-run of the failures, never by the first parallel pass — xdist-parallel runs on this box reclassify a rotating set of load-induced flakes as green on serial rerun; real failures persist. Reset (`stop-all` + `brutal_teardown.sh` + `pkill -x nginx`) between heavy runs — the box itself degrades under sustained repeated load (orphaned masters, climbing load average) in a way that makes diagnosis on a degraded box unreliable.

## 7. Tooling language policy — Python replaces bash, and the bash original gets deleted

Rob has instructed repeatedly, across several separate efforts, that shell scripts in
this repo be rewritten in Python and the `.sh` originals **removed**, not left beside
the port. It applied to the whole test fleet (`tests/manage_test_servers.sh` and its
satellites → `cmdscripts/` + `RegistryLauncher`) and applies equally to the
`tools/ci/check_*.sh` guard fleet. The reasoning is bash's unsafe word-splitting and
quoting, locale-dependent `sort`, and the fact that a shell guard cannot be unit-tested
the way `tests/test_source_guards.py` tests the Python ones.

The corollary is the part that has gone wrong before: **"it's bash for consistency with
the sibling guards" is not a valid reason to keep a `.sh`.** The goal is to eliminate
bash from the tree, so converting one guard makes converting its siblings the *right*
follow-on, not a consistency violation to be avoided.

How to do the port faithfully: keep the contract identical (same stdout messages, same
exit codes, same backlog-file format), rewire every caller (`.github/workflows/guards.yml`,
`tools/ci/README.md`, `tests/source_guards_lib.py`, and the sibling guards' header
comments), run both the guard itself and `tests/test_source_guards.py`, then delete the
`.sh`. Prefer importing shared logic in-process (the `tools/readability.py` pattern) over
shelling out to a subprocess, and use explicit codepoint ordering rather than relying on
the ambient locale the way bash's `sort` does.

## 8. Who Rob is and what this project is for

Rob Currie is a HEP (High Energy Physics) systems engineer building **nginx-xrootd** (rebranded mid-project to **BriX**) — an nginx module serving the native XRootD `root://` binary protocol (stream module) alongside WebDAV over HTTPS (`davs://`), S3, and CVMFS, with the long-run direction of a WLCG-storage "swiss-army-knife" (native clients, FUSE mount, broad protocol/backend coverage) rather than a narrow gateway replacement. The original, still-live deployment driver: replacing a lightweight production XRootD gateway at a real HEP site backed by CephFS, where VO (Virtual Organization)-based access control via VOMS proxy certificates, group/GID inheritance under paths (CephFS doesn't reliably propagate setgid), Prometheus metrics visibility, comprehensive test coverage, and a better TCP stack than standalone XRootD were the explicit priorities from day one.

Rob works at the CLI level, expects concise responses, and reviews diffs rather than reading prose summaries — keep reports terse and code-forward. He runs multiple concurrent Claude sessions against the same working tree regularly (a fact that directly motivates the git-write and subagent-dispatch rules above), and he himself commits straight to `main` with no branches.

---

*This document synthesizes memory files spanning 2026-04-16 through 2026-07-15. Files skimmed as meta-indexes rather than transcribed in full: `archive_index_2026_07_13.md` and `INDEX-ARCHIVE.md` (both are superseded pointer-indexes of the memory system itself, not project substance — retained value was cross-checking that no build/refactor/rule item here was missing from the record). No memory file in the assigned set was empty or had literally nothing worth keeping; `code_reduction_opportunities.md` and `phase17_macro_collapse.md`/`phase27_status.md`/`phase47_operability_packaging.md` were lower-signal (superseded proposals or already covered by other landed-work entries) and are folded into single sentences above rather than given their own subsections.*
