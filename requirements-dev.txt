# Tooling for the tools/ci guard fleet and the analysis lanes. Not needed to
# run the test suite; needed to reproduce CI's static gates locally.
#
#   pip install --user -r requirements-dev.txt
#
# The guards self-skip (with an explanatory message) when their tool is absent,
# so a missing entry here degrades to "not checked", never to a false green —
# which is precisely why they are pinned rather than left floating: a new major
# changing a metric's definition would move a ratchet baseline under us.
# Bounds policy matches requirements.txt: lower AND upper, both reviewed.

# --- static analysis lane: tools/ci/run_codechecker.py (needs clang-tidy) ----
codechecker>=6.28.2,<7
